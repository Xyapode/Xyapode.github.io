{% extends "base.html.twig" %}

{% block body %}

    <div class="row">
        <div class="col-8 mx-auto">
            {{ form_start(form, {'attr': {novalidate: 'novalidate'}} ) }}

                <div class="mb-3">
                    {{ form_row(form.title) }}
                </div>

                <div class="mb-3">
                    {{ form_row(form.content) }}
                </div>

                <div class="mb-3">
                    {{ form_row(form.photo) }}
                </div>

                <button type="submit" class="d-block col-2 mx-auto btn btn-warning mb-3">Publier</button>
        </div>    
    </div>

{% endblock %}